// you solution should be here
//Solutions to 1
var birthYear = 1995;
var futureYear = 2020;
var numYears = futureYear - birthYear

console.log('I will be either ' + numYears + ' or ' + (numYears - 1));

//Solutions to 2
var currentAge = 24;
var maxAge = 100;
var numPerDay = 3;
var totalNeeded = (numPerDay * 365) * (maxAge - currentAge);
var message = 'You will need ' + totalNeeded + ' cups of tea to last you until the ripe old age of ' + maxAge;
console.log(message);
